apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - configmap.yaml
  - deployment.yaml
  - service.yaml
  - homelab-chat-api-secrets-sealed.yaml

labels:
  - pairs:
      app: homelab-chat-api
    includeSelectors: false
    includeTemplates: true
