---
apiVersion: v1
kind: Service
metadata:
  name: gasa-web
  namespace: gasa
  labels:
    app: gasa-web
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
  selector:
    app: gasa-web
