---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: oauth2-proxy-claude-mcp
  namespace: oauth2-proxy
spec:
  replicas: 2
  selector:
    matchLabels:
      app: oauth2-proxy-claude-mcp
  template:
    metadata:
      labels:
        app: oauth2-proxy-claude-mcp
    spec:
      containers:
        - name: oauth2-proxy
          image: quay.io/oauth2-proxy/oauth2-proxy:v7.7.1
          args:
            - --provider=oidc
            - --oidc-issuer-url=https://authentik.k12n.com/application/o/oauth2-proxy/
            - --client-id=claude-mcp
            - --client-secret=$(OAUTH2_PROXY_CLIENT_SECRET)
            - --cookie-secret=$(OAUTH2_PROXY_COOKIE_SECRET)
            - --cookie-secure=true
            - --cookie-httponly=true
            - --cookie-samesite=none
            - --cookie-name=_oauth2_proxy_claude_mcp
            - --cookie-domain=.k12n.com
            - --whitelist-domain=.k12n.com
            - --reverse-proxy=true
            - --real-client-ip-header=X-Forwarded-For
            - --email-domain=*
            - --http-address=0.0.0.0:4180
            - --upstream=static://202
            - --skip-provider-button=true
            - --pass-authorization-header=true
            - --pass-access-token=true
            - --set-authorization-header=true
            - --set-xauthrequest=true
            - --scope=openid profile email
            - --proxy-prefix=/oauth2/claude
            - --redirect-url=https://api.k12n.com/oauth2/claude/callback
          env:
            - name: OAUTH2_PROXY_CLIENT_SECRET
              valueFrom:
                secretKeyRef:
                  name: oauth2-proxy-claude-mcp-secrets
                  key: client-secret
            - name: OAUTH2_PROXY_COOKIE_SECRET
              valueFrom:
                secretKeyRef:
                  name: oauth2-proxy-claude-mcp-secrets
                  key: cookie-secret
          ports:
            - containerPort: 4180
              name: http
          livenessProbe:
            httpGet:
              path: /ping
              port: 4180
            initialDelaySeconds: 10
          readinessProbe:
            httpGet:
              path: /ready
              port: 4180
            initialDelaySeconds: 5
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 200m
              memory: 128Mi
