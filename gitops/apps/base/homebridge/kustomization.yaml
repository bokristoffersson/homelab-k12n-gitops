apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - pvc.yaml
  - configmap.yaml
  - deployment.yaml
  - service.yaml
  - homebridge-mqtt-credentials-sealed.yaml

labels:
  - pairs:
      app: homebridge
    includeSelectors: false
    includeTemplates: true
