apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - pvc.yaml
  - configmap.yaml
  - minecraft-rcon-sealed.yaml
  - deployment.yaml
  - service.yaml

labels:
  - pairs:
      app: minecraft
    includeSelectors: false
    includeTemplates: true
