# Minecraft (itzg/minecraft-server) config. Rendered via kustomize configMapGenerator
# so any change produces a new hashed ConfigMap name → rolling restart (pods do not
# hot-reload envFrom ConfigMaps; see CLAUDE.md "After ConfigMap updates").
EULA=TRUE
TYPE=PAPER
VERSION=26.2
MEMORY=3G
USE_AIKAR_FLAGS=TRUE
ONLINE_MODE=TRUE
ENABLE_WHITELIST=TRUE
ENFORCE_WHITELIST=TRUE
# Comma-separated Minecraft usernames (whitelist + server operators/admins).
WHITELIST=WizardMuffin199
OPS=WizardMuffin199
DIFFICULTY=normal
MODE=survival
MOTD=k12n homelab
MAX_PLAYERS=10
VIEW_DISTANCE=10
SIMULATION_DISTANCE=8
TZ=Europe/Stockholm
ENABLE_RCON=TRUE
RCON_PORT=25575
OVERRIDE_SERVER_PROPERTIES=TRUE
