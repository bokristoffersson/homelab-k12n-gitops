apiVersion: v1
kind: Namespace
metadata:
  name: minecraft
  labels:
    name: minecraft
    # Explicit PSA (cluster default is baseline). Minecraft runs as uid 1000,
    # no hostNetwork/privileged — baseline is sufficient.
    pod-security.kubernetes.io/enforce: baseline
    pod-security.kubernetes.io/warn: restricted
