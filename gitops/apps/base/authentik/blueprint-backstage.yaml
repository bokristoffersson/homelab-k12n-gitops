---
apiVersion: v1
kind: ConfigMap
metadata:
  name: blueprint-backstage
  namespace: authentik
  labels:
    app.kubernetes.io/name: authentik-blueprints
data:
  backstage.yaml: |
    version: 1
    metadata:
      name: backstage
    entries:
      # OAuth2 Provider for Backstage
      - model: authentik_providers_oauth2.oauth2provider
        identifiers:
          name: backstage
        attrs:
          authorization_flow: !Find [authentik_flows.flow, [slug, default-provider-authorization-implicit-consent]]
          invalidation_flow: !Find [authentik_flows.flow, [slug, default-invalidation-flow]]
          client_type: confidential
          client_id: backstage
          redirect_uris:
            - matching_mode: strict
              url: "https://backstage.k12n.com/api/auth/oidc/handler/frame"
          signing_key: !Find [authentik_crypto.certificatekeypair, [name, "authentik Self-signed Certificate"]]
          access_code_validity: minutes=1
          access_token_validity: hours=24
          refresh_token_validity: days=30
          issuer_mode: per_provider
          sub_mode: hashed_user_id
          include_claims_in_id_token: true
          property_mappings:
            - !Find [authentik_providers_oauth2.scopemapping, [scope_name, openid]]
            - !Find [authentik_providers_oauth2.scopemapping, [scope_name, email]]
            - !Find [authentik_providers_oauth2.scopemapping, [scope_name, profile]]

      # Application
      - model: authentik_core.application
        identifiers:
          slug: backstage
        attrs:
          name: Backstage
          provider: !Find [authentik_providers_oauth2.oauth2provider, [name, backstage]]
          meta_launch_url: https://backstage.k12n.com
