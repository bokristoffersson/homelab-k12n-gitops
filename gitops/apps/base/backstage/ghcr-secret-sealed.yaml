---
# NOTE: This is a placeholder. You need to create the actual sealed secret using kubeseal.
# 
# To create this file:
# 1. Create a temporary secret file (ghcr-secret.yaml):
#    apiVersion: v1
#    kind: Secret
#    metadata:
#      name: ghcr-secret
#      namespace: backstage
#    type: kubernetes.io/dockerconfigjson
#    data:
#      .dockerconfigjson: <base64-encoded-docker-config>
#
# 2. Seal it:
#    kubeseal -f ghcr-secret.yaml -o ghcr-secret-sealed.yaml
#
# 3. Replace this file with the generated sealed secret
#
# OR reuse an existing GHCR secret pattern from mqtt-input/ghcr-secret-sealed.yaml
# and update the namespace to 'backstage'

apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: ghcr-secret
  namespace: backstage
spec:
  encryptedData:
    .dockerconfigjson: PLACEHOLDER_REPLACE_WITH_SEALED_SECRET
  template:
    metadata:
      name: ghcr-secret
      namespace: backstage
    type: kubernetes.io/dockerconfigjson








