---
# NOTE: This is a placeholder. You need to create the actual sealed secret using kubeseal.
#
# To create this file:
# 1. Create a temporary secret file (database-secret.yaml):
#    apiVersion: v1
#    kind: Secret
#    metadata:
#      name: backstage-database-secret
#      namespace: backstage
#    type: Opaque
#    data:
#      POSTGRES_USER: <base64-encoded-username>
#      POSTGRES_PASSWORD: <base64-encoded-password>
#      POSTGRES_DB: <base64-encoded-database-name>
#
#    You can get the existing TimescaleDB credentials:
#    kubectl get secret timescaledb-secret -n heatpump-mqtt -o jsonpath='{.data.POSTGRES_USER}' | base64 -d && echo
#    kubectl get secret timescaledb-secret -n heatpump-mqtt -o jsonpath='{.data.POSTGRES_PASSWORD}' | base64 -d && echo
#
#    Set POSTGRES_DB to: backstage (base64 encoded: YmFja3N0YWdl)
#
# 2. Seal it:
#    kubeseal -f database-secret.yaml -o database-secret-sealed.yaml
#
# 3. Replace this file with the generated sealed secret

apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: backstage-database-secret
  namespace: backstage
spec:
  encryptedData:
    POSTGRES_USER: PLACEHOLDER_REPLACE_WITH_SEALED_SECRET
    POSTGRES_PASSWORD: PLACEHOLDER_REPLACE_WITH_SEALED_SECRET
    POSTGRES_DB: PLACEHOLDER_REPLACE_WITH_SEALED_SECRET
  template:
    metadata:
      name: backstage-database-secret
      namespace: backstage








