---
# NOTE: This is a placeholder. You need to create the actual sealed secret using kubeseal.
#
# To create this file:
# 1. Create a GitHub OAuth App at https://github.com/settings/developers
#    - Homepage URL: https://backstage.k12n.com
#    - Authorization callback URL: https://backstage.k12n.com/api/auth/github/handler/frame
#
# 2. Create a temporary secret file (github-oauth-secret.yaml):
#    apiVersion: v1
#    kind: Secret
#    metadata:
#      name: github-oauth-secret
#      namespace: backstage
#    type: Opaque
#    data:
#      CLIENT_ID: <base64-encoded-client-id>
#      CLIENT_SECRET: <base64-encoded-client-secret>
#
# 3. Seal it:
#    kubeseal -f github-oauth-secret.yaml -o github-oauth-secret-sealed.yaml
#
# 4. Replace this file with the generated sealed secret

apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: github-oauth-secret
  namespace: backstage
spec:
  encryptedData:
    CLIENT_ID: PLACEHOLDER_REPLACE_WITH_SEALED_SECRET
    CLIENT_SECRET: PLACEHOLDER_REPLACE_WITH_SEALED_SECRET
  template:
    metadata:
      name: github-oauth-secret
      namespace: backstage








