---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: frontend-routes
  namespace: traefik
spec:
  entryPoints:
    - web
  routes:
    # OAuth2 Proxy Callbacks (public - must come BEFORE protected routes)
    - match: Host(`heatpump.k12n.com`) && PathPrefix(`/oauth2`)
      kind: Rule
      priority: 100  # Higher priority to match first
      middlewares:
        - name: https-scheme
          namespace: traefik
      services:
        - name: oauth2-proxy
          namespace: oauth2-proxy
          port: 4180

    # Heatpump Web Frontend (protected by OAuth2 Proxy)
    - match: Host(`heatpump.k12n.com`)
      kind: Rule
      middlewares:
        - name: https-scheme
          namespace: traefik
        - name: oauth2-proxy-auth
          namespace: traefik
      services:
        - name: heatpump-web
          namespace: heatpump-web
          port: 80
