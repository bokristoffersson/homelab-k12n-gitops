---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: api-routes
  namespace: traefik
spec:
  entryPoints:
    - web  # Port 8000
  routes:
    # Homelab API (protected by JWT tokens from Authentik)
    - match: Host(`api.k12n.com`) && PathPrefix(`/api/v1`)
      kind: Rule
      middlewares:
        - name: cors
          namespace: traefik
        - name: security-headers
          namespace: traefik
      services:
        - name: homelab-api
          namespace: homelab-api
          port: 8080

    # Energy WebSocket (JWT auth via query parameter)
    - match: Host(`api.k12n.com`) && PathPrefix(`/ws`)
      kind: Rule
      middlewares:
        - name: cors
          namespace: traefik
      services:
        - name: homelab-energy-ws
          namespace: energy-ws
          port: 8080

    # OAuth2 Proxy Callbacks (public)
    - match: Host(`api.k12n.com`) && PathPrefix(`/oauth2`)
      kind: Rule
      middlewares:
        - name: cors
          namespace: traefik
      services:
        - name: oauth2-proxy
          namespace: oauth2-proxy
          port: 4180
