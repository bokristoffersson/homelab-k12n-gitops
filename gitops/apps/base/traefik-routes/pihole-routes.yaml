---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: pihole-routes
  namespace: traefik
spec:
  entryPoints:
    - web
  routes:
    # Pi-hole DNS Admin (Pi-hole has built-in login)
    - match: Host(`pihole.k12n.com`)
      kind: Rule
      middlewares:
        - name: security-headers
          namespace: traefik
      services:
        - name: pihole-web
          namespace: pihole
          port: 80
