apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - timescaledb-credentials-sealed.yaml
  - spotprice-apns-sealed.yaml
  - configmap.yaml
  - deployment.yaml
  - service.yaml

labels:
  - pairs:
      app: spotprice-api
    includeSelectors: false
    includeTemplates: true
