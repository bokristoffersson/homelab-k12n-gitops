---
# NOTE: This is a placeholder. You need to create the actual sealed secret using kubeseal.
# 
# To create this file:
# 1. Create a temporary secret file (ghcr-secret.yaml):
#    apiVersion: v1
#    kind: Secret
#    metadata:
#      name: ghcr-secret
#      namespace: heatpump-web
#    type: kubernetes.io/dockerconfigjson
#    data:
#      .dockerconfigjson: <base64-encoded-docker-config-json>
#
# 2. Seal the secret:
#    kubeseal -f ghcr-secret.yaml -o ghcr-secret-sealed.yaml
#
# 3. Replace this file with the sealed secret output

apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: ghcr-secret
  namespace: heatpump-web
spec:
  encryptedData:
    # PLACEHOLDER - Replace with actual sealed secret
    .dockerconfigjson: AgBy3i4OJSWK+PiTy4ZZA==
  template:
    metadata:
      name: ghcr-secret
      namespace: heatpump-web
    type: kubernetes.io/dockerconfigjson



