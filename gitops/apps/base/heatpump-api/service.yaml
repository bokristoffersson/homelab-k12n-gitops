apiVersion: v1
kind: Service
metadata:
  name: heatpump-api
  namespace: heatpump-api
  labels:
    app: heatpump-api
spec:
  selector:
    app: heatpump-api
  ports:
    - name: http
      port: 3000
      targetPort: 3000
      protocol: TCP
  type: ClusterIP

