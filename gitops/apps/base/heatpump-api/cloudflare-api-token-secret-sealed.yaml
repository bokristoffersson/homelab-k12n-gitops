---
# NOTE: This file is kept for reference, but the actual secret is now in cloudflare-tunnel namespace
# See: gitops/infrastructure/controllers/cloudflare-tunnel/cloudflare-api-token-secret-sealed.yaml
#
# The secret is stored in cloudflare-tunnel namespace (infrastructure-level) and
# referenced by the setup job in heatpump-api namespace via cross-namespace secret reference.
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: cloudflare-api-token
  namespace: heatpump-api
spec:
  encryptedData:
    # DEPRECATED: Secret moved to cloudflare-tunnel namespace
    # See gitops/infrastructure/controllers/cloudflare-tunnel/cloudflare-api-token-secret-sealed.yaml
    api-token: PLACEHOLDER_DEPRECATED
    account-id: PLACEHOLDER_DEPRECATED
  template:
    metadata:
      name: cloudflare-api-token
      namespace: heatpump-api

