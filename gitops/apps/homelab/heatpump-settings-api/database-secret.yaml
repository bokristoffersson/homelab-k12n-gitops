# This is a placeholder - needs to be sealed with kubeseal
# Run: echo -n 'postgres' | kubectl create secret generic database-credentials \
#   --dry-run=client \
#   --from-literal=username=postgres \
#   --from-literal=password='<PASSWORD_HERE>' \
#   -o yaml | kubeseal --controller-name=sealed-secrets --controller-namespace=sealed-secrets -o yaml > database-secret-sealed.yaml
apiVersion: v1
kind: Secret
metadata:
  name: database-credentials
  namespace: heatpump-settings
type: Opaque
stringData:
  username: postgres
  password: PLACEHOLDER_REPLACE_WITH_SEALED_SECRET
