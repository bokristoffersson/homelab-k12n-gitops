apiVersion: longhorn.io/v1beta2
kind: Node
metadata:
  name: p1 # node name for node with nvme storage
  namespace: longhorn-system
spec:
  allowScheduling: true
  disks:
    nvme-disk:
      allowScheduling: true
      path: /mnt/data/nvme-longhorn
      storageReserved: 10737418240  # 10Gi reserved
      tags:
        - nvme
        - fast
  tags:
    - nvme-node