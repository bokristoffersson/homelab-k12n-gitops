apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # Core controllers
  - ../controllers/cert-manager
  - ../controllers/flux-system

  # Traefik ingress controller
  - traefik

# Skip sealed-secrets for local development
# Skip cloudflare-tunnel for local development
