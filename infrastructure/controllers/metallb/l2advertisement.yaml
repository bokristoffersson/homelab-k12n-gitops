apiVersion: metallb.io/v1beta1
kind: L2Advertisement
metadata:
  name: default-l2-adv
  namespace: metallb-system
spec:
  ipAddressPools:
    - default-pool

  # Optional: Advertise only from specific nodes
  # nodeSelectors:
  # - matchLabels:
  #     node-role.kubernetes.io/worker: "true"

  # Optional: Advertise only on specific interfaces
  # interfaces:
  # - eth0
  # - ens192