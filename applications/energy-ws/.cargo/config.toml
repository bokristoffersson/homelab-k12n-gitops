[build]
# Use a target directory without spaces to avoid issues with rdkafka-sys build scripts
# Using /tmp which exists in both local and cluster environments
target-dir = "/tmp/cargo-target/energy-ws"

